language: node_js

node_js:
  - "6"

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - npm install -g yarn
  - yarn

script:
  - yarn lint
  - yarn test
  - yarn build
  - yarn start
  - yarn webdriver
  - yarn test:e2e
